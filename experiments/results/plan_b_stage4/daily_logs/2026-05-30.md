# 2026-05-30

## What changed today

- Changed the downstream checkpoint reporting policy to use a single common checkpoint across all A/B/C/D/E splits: `checkpoint-step-1500`.
- Rationale: `checkpoint-step-1500` is the best E Stage4 checkpoint in the short-answer VQAv2 sweep, and using the same checkpoint for every split is easier to defend than selecting a different per-split oracle checkpoint.

## Experiments started / finished

- Continued `Stage4ABCDCheckpointSweep` on the Windows RTX 3090 machine.
- At 2026-05-30 00:07 CST, `D_naive_union checkpoint-step-1500` was actively running. Once it finishes, the common-1500 A/B/C/D/E comparison table has all required rows.
- Completed `Stage4ABCDCheckpointSweep` at 2026-05-30 00:55 CST.
- Common `checkpoint-step-1500` VQAv2 short-answer results:
  - A raw: strict VQA `0.0497`, relaxed VQA `0.6330`.
  - B image-only: strict VQA `0.1073`, relaxed VQA `0.6383`.
  - C text-only: strict VQA `0.0267`, relaxed VQA `0.4167`.
  - D naive-union: strict VQA `0.0013`, relaxed VQA `0.3043`.
  - E Stage4: strict VQA `0.2277`, relaxed VQA `0.5897`.
- Synced the completed Windows VQAv2 checkpoint-eval metrics/predictions/logs back to the Mac and mirrored them under `experiments/results/plan_b_stage4/windows_sync/`.
- Created paper-summary tables under `experiments/results/plan_b_stage4/icdm_revision/summary_20260530/`:
  - `stage4_main_eval_3000.csv`
  - `training_splits_v2.csv`
  - `llava_train25k_v2.csv`
  - `llava_vqav2_checkpoint_sweep.csv`
  - `llava_vqav2_common_ckpt1500.csv`
  - `llava_vqav2_best_strict_by_split.csv`
  - `paper_ready_summary.json`
- Added `--sample-before-slice` to `experiments/scripts/run_llava_stage4_lora.py` so future paper-facing 25K runs can sample the full split with a fixed seed before applying the 25K budget.
- Queued `Stage4V2HeldoutEval` on the Windows RTX 3090. It prepares a 1000-question VQAv2 held-out subset excluding the quick-dev question ids, then evaluates A/B/C/D/E `checkpoint-step-1500` plus a no-adapter baseline.
- Queued `Stage4Random25KQueue` on the Windows RTX 3090. It waits for held-out eval to finish, then reruns A/B/C/D/E 25K training with `--sample-before-slice --seed 20260530`, checkpoints every 100 steps, and evaluates held-out VQAv2 at checkpoint-step-1500.
- Ran `exp_textvqa_data_probe_20260530`; status is `blocked_missing_textvqa_data` because no TextVQA files were found under the expected data/eval roots.
- Created `exp_stage4_efficiency_summary_20260530` from existing source metrics and appended 11 completed v2 training/eval/efficiency rows to `experiment_ledger.csv`.

## Current blockers

- `Stage4V2HeldoutEval` is running; wait for held-out VQAv2 metrics before treating downstream as paper-facing.
- `Stage4Random25KQueue` is queued behind held-out eval and will run for many hours if uninterrupted.
- TextVQA remains blocked until question/annotation/image files are available.

## Tomorrow's first priority

- Monitor `Stage4V2HeldoutEval`; once complete, sync held-out metrics back to Mac, add rows to the ledger, regenerate dashboard, and decide whether the random-sampled rerun should continue or be deprioritized.

## Decisions made

- Use strict `vqa_consensus` as the paper-facing VQAv2 metric because it is closest to the official VQA accuracy protocol.
- Report `relaxed_vqa_consensus` only as an auxiliary diagnostic for caption-style long-answer undercounting.
- Use the common `checkpoint-step-1500` downstream table as the cleaner fairness story; keep per-split best and final-checkpoint results as diagnostic appendices.

## Final summary draft

- Generated the final experiment summary document at `output/doc/mmdedup_pipeline_revision_final_summary_20260530.docx`.
- The document updates the old revision-plan report with the current 3,000-label Stage 4 main evaluation, A/B/C/D/E training split counts, LLaVA train-loss summary, VQAv2 quick/dev common-1500 results, VQAv2 held-out 1000 common-1500 results, and paper caveats.
- Key wording preserved for the paper: main Stage 4 evaluation measures detection quality with `joint>=0.85`; downstream training uses the conservative operating point `image>=0.85 AND text>=0.85`.
- Key caveat preserved for the paper: no-adapter LLaVA remains much stronger than all CC3M-caption LoRA adapters on VQAv2, so downstream results should be framed as retention under identical caption tuning, not improvement over the base LLaVA model.
- Fixed the Word list-marker compatibility issue in the final summary document by replacing automatic bullet paragraphs with literal `-` prefixes; kept a backup at `output/doc/mmdedup_pipeline_revision_final_summary_20260530_before_listfix.docx`.
- Re-fixed the list-marker issue after WPS/Word still showed leading rectangles: removed symbol prefixes entirely and converted all summary bullets to explicit plain-text `1.`, `2.`, `3.` numbering; kept a second backup at `output/doc/mmdedup_pipeline_revision_final_summary_20260530_before_numberfix.docx`.
- Created `output/doc/mmdedup_pipeline_revision_final_summary_20260530_clean_numbered.docx` and replaced the original WeChat temp source document with the same clean-numbered content, because the user was still opening the old temp copy.
- Updated the final summary document again after review comments: marked D training runtime as `resume-only` instead of comparable full runtime, clarified that the 3,000 fair labels are score-space stratified and not population-distribution estimates, strengthened semantic-independence wording for human labels, separated `E_eval` from `E_train`, marked prefix-sliced downstream results as preliminary, replaced "significant" wording with non-statistical improvement language, and adjusted threshold table wrapping.
- Clarified the D runtime note in the final summary: v2 D full runtime is not cleanly recorded because the run was interrupted/resumed; `0.54h` is only the final checkpoint-step-1900-to-2000 resume segment. The old 20260521 full D run is available as a reference (`21,072s` / `5.85h`) but should not replace v2 source-of-truth.
- Updated the final summary to use an estimated D v2 full runtime of about `5.8h`, derived from the completed v2 A/B/C/E average (`5.70h`) and the old full D reference (`5.85h`), while keeping the `0.54h` final resume segment explicitly marked.
- Added the explanation for the estimated D runtime directly in the final summary: same hardware, same 25K samples, same 2000 steps, same LoRA/4-bit setup; estimate is for training-cost scale only and does not replace strict source-of-truth runtime.
- Tightened final summary wording after review: D runtime is shown as `N/A; resume=0.54h; estimated≈5.8h`, checkpoint-step-1500 is no longer described as stable, naive-union downstream weakness is framed as a possible signal rather than proof, E names are standardized to `E_eval_stage4_joint` and `E_train_stage4_conservative`, and the held-out VQAv2 summary explicitly says it is under the current prefix-sliced 25K setting.
- Final wording polish: changed "strongest paper-facing evidence" to "main paper-facing evidence", changed `Pred.+ rate` cells to `N/A (stratified eval)`, removed Chinese typographic quotes that rendered poorly, and weakened the checkpoint sentence to "under the unified checkpoint-step-1500 comparison, E performs best."
